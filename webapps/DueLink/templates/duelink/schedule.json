{"events":[
  {% for event in events %}
  {%spaceless%}
  {
    "title": "{{event.deadline.name}}",
    "start": "{{event.deadline.due |date:'c'}}"
  }
  {%endspaceless%}
  {% if not forloop.last %},{% endif%}
  {% endfor %}
]}
